/**
 Dados dos números y un operador aritmético (su nombre, como una cadena), devuelve el resultado de los dos números que tienen
ese operador utilizado en ellos.

`a` y `b` serán números enteros positivos, y ``a` siempre será el primer número de la operación, y ``b` siempre el segundo.

Los cuatro operadores son "suma", "resta", "división", "multiplicación".
 
*/